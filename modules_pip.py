#modules are a python file that you can import into a current python file

import random_tools

print(random_tools.roll_dice(6))

#can install python modules using pip
#pip is a program that allows you to install python modules
#pip is a packet manager that allows you to install manage update uninstall different python modules

#inside the terminal can use pip install commands